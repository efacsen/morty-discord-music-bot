---
phase: 01-typescript-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - eslint.config.js
  - src/index.ts
autonomous: true
requirements:
  - TSF-03
gap_closure: true

must_haves:
  truths:
    - "`npm run dev` starts a live `tsx` watch session from `src/index.ts` and remains running until deliberately stopped"
    - "`npm run dev` no longer fails because `tsx` is missing or because `src/index.ts` is absent"
    - "`npm run build` still exits 0 and preserves `dist/index.js` as the production entrypoint for `npm start`"
  artifacts:
    - path: "package.json"
      provides: "Explicit tsx devDependency and the locked Phase 1 dev script contract"
      contains: "\"dev\": \"tsx watch src/index.ts\""
    - path: "src/index.ts"
      provides: "Phase 1 TypeScript watch entrypoint shim"
      contains: "import './index.js'"
    - path: "tsconfig.json"
      provides: "Compiler exclusion preventing duplicate dist/index.js output during the mixed JS/TS phase"
      contains: "\"src/index.ts\""
  key_links:
    - from: "package.json:scripts.dev"
      to: "node_modules/.bin/tsx"
      via: "explicit devDependency install"
      pattern: "\"tsx\":"
    - from: "package.json:scripts.dev"
      to: "src/index.ts"
      via: "tsx watch target"
      pattern: "\"dev\": \"tsx watch src/index.ts\""
    - from: "src/index.ts"
      to: "src/index.js"
      via: "ESM shim import"
      pattern: "import './index.js'"
---

<objective>
Close the Phase 1 verification gap for TSF-03 by making the promised watch-mode entrypoint actually runnable without pulling the full source migration into this phase.

Purpose: Phase 2 depends on a working no-manual-build dev loop. Right now the repo advertises `tsx watch src/index.ts`, but the binary is missing and the file does not exist.
Output: explicit `tsx` installation, a Phase 1-safe `src/index.ts` watch shim, and compiler/lint scope updates that avoid `dist/index.js` collisions
</objective>

<execution_context>
@/Users/kevinzakaria/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kevinzakaria/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-typescript-foundation/01-CONTEXT.md
@.planning/phases/01-typescript-foundation/01-VERIFICATION.md
@.planning/phases/01-typescript-foundation/01-01-SUMMARY.md
@.planning/phases/01-typescript-foundation/01-02-SUMMARY.md

@package.json
@tsconfig.json
@eslint.config.js
@src/index.js

<interfaces>
From package.json:
```json
"start": "node dist/index.js",
"dev": "tsx watch src/index.ts",
"build": "tsc"
```

From src/index.js:
```javascript
await checkDavey()
client.login(process.env.DISCORD_CLIENT_TOKEN)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install tsx explicitly and preserve the locked dev script contract</name>
  <files>package.json, package-lock.json</files>
  <action>
1. Add `tsx` to `devDependencies` using the package manager so the repo has a project-local executable. Do not leave `tsx` as a transitive-only dependency.

2. Keep the user-locked script contract exactly as-is:
   - `dev`: `tsx watch src/index.ts`
   - `build`: `tsc`
   - `start`: `node dist/index.js`

3. Do not change any other Phase 1 requirement coverage while fixing this gap. This task is only restoring the missing executable promised by TSF-03.
  </action>
  <verify>
    <automated>cd /Users/kevinzakaria/developers/discord-music-bot && npm ls tsx --depth=0 >/dev/null && node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('package.json','utf8')); if(p.scripts.dev!=='tsx watch src/index.ts') process.exit(1); if(p.scripts.build!=='tsc') process.exit(1); if(p.scripts.start!=='node dist/index.js') process.exit(1); if(!p.devDependencies?.tsx) process.exit(1)"</automated>
  </verify>
  <done>`package.json` and `package-lock.json` record `tsx` explicitly, and the build/dev/start script contract remains exactly aligned with the Phase 1 user decisions</done>
</task>

<task type="auto">
  <name>Task 2: Add a Phase 1-safe TypeScript watch entrypoint without breaking build output</name>
  <files>tsconfig.json, eslint.config.js, src/index.ts</files>
  <action>
1. Create `src/index.ts` as a minimal watch-mode shim that imports the existing JavaScript bootstrap from `./index.js`. Do not duplicate the current startup logic in TypeScript and do not migrate commands, events, or helpers here. This file exists only to satisfy the locked `tsx watch src/index.ts` contract during Phase 1.

2. Update `tsconfig.json` so `src/index.ts` does not collide with the existing `src/index.js` output during `npm run build`. Keep all locked compiler settings unchanged (`strict: true`, `module: "NodeNext"`, `allowJs: true`, `checkJs: false`). The change should be limited to the include/exclude surface needed to prevent two source entrypoints from both trying to emit `dist/index.js`.

3. Update `eslint.config.js` only as much as needed to keep Phase 1 linting green after the shim is added. Do not widen lint coverage and do not change the chosen presets. If the shim is intentionally excluded from the active TS project, ignore it explicitly rather than reworking the flat-config architecture.

4. Preserve the current production bootstrap path: `npm run build` must still produce `dist/index.js`, and `npm start` must still execute that compiled file.

5. The automated verification for this task must actually launch `npm run dev`, wait long enough for `tsx watch` to start supervising the entrypoint, assert that the watch process is still alive with `kill -0 $PID`, and then stop it intentionally. Do not treat successful Discord login as part of this proof. Runtime auth or network errors are acceptable as long as the watch supervisor stays alive until killed.
  </action>
  <verify>
    <automated>cd /Users/kevinzakaria/developers/discord-music-bot && npm run build >/dev/null && npm run lint >/dev/null && rm -f /tmp/morty-phase01-dev.log && npm run dev >/tmp/morty-phase01-dev.log 2>&1 & PID=$!; sleep 5; kill -0 "$PID"; STATUS=$?; kill "$PID" >/dev/null 2>&1 || true; wait "$PID" >/dev/null 2>&1 || true; node -e "const fs=require('fs'); const shim=fs.readFileSync('src/index.ts','utf8'); const tsconfig=fs.readFileSync('tsconfig.json','utf8'); if(!shim.includes(\"import './index.js'\")) process.exit(1); if(!tsconfig.includes('src/index.ts')) process.exit(1)"; exit $STATUS</automated>
  </verify>
  <done>`src/index.ts` exists as a thin shim to the legacy JS bootstrap, the compiler no longer has a duplicate `dist/index.js` emission path, lint/build remain green, and `npm run dev` reaches a live watch session that stays up until intentionally killed</done>
</task>

</tasks>

<verification>
Run the Phase 1 closure checks after both tasks complete:

```bash
cd /Users/kevinzakaria/developers/discord-music-bot
npm run build
npm run lint
npm ls tsx --depth=0
node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('package.json','utf8')); if(p.scripts.dev!=='tsx watch src/index.ts') process.exit(1); if(!fs.existsSync('src/index.ts')) process.exit(1)"
```

Then perform the watch-mode smoke check that proves the roadmap truth directly:

```bash
cd /Users/kevinzakaria/developers/discord-music-bot
rm -f /tmp/morty-phase01-dev.log
npm run dev >/tmp/morty-phase01-dev.log 2>&1 &
PID=$!
sleep 5
kill -0 "$PID"
kill "$PID" >/dev/null 2>&1 || true
wait "$PID" >/dev/null 2>&1 || true
```

`kill -0 "$PID"` is the proof point: `tsx watch` reached a live supervised session and stayed running until deliberately stopped. Do not require successful Discord login for this verification.
</verification>

<success_criteria>
- `package.json` has an explicit `tsx` devDependency and still advertises `tsx watch src/index.ts`
- `src/index.ts` exists and forwards to the current JS bootstrap instead of starting a premature migration
- `npm run build` exits 0 after the shim is added
- `npm run lint` exits 0 after the shim is added
- The automated smoke check proves `npm run dev` reaches a live watch session and remains alive until intentionally killed, without depending on real Discord connectivity
</success_criteria>

<output>
After completion, create `.planning/phases/01-typescript-foundation/01-03-SUMMARY.md`
</output>
